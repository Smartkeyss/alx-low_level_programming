#include <stdio.h>

/**
 * main - input the ascii value
 * output the hexadecimal of the number
 * Return: Always 0 (success)
 */

int main(void)
{
	int x;

	int n;

	for (x == 0 ; x <= 9; x++)
	{
		putchar(x);
	}

	for (n = 97; n <= 103; n++)
	{
		putchar(n);
	}
	putchar(10);

	return (0);
}
