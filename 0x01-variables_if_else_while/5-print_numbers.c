#include <stdio.h>
#include <stdlib.h>
/**
 * main -> Input the ASCII number corresponding digits
 * print output
 * Return: always (0) success
 */

int main (void)

{
	int x;

	for (x == 0; x < 10; x++)
	{
		putchar(x);
	}
	putchar(10);
	return (0);
}

